# cs-ecommerce-nodejs

Node.js min version: 7.6.0

To run all services
    docker-compose up -d

Consul UI - > http://localhost:8500

RabbitMQ UI -> http://localhost:8080
    User -> user
    Password -> password
